# Python dependencies for this poem's code. Pin versions for reproducibility.
# Only the data-acquisition scripts need these; main.py and build_web.py use the
# standard library only.
requests==2.32.5
pandas==2.2.3
lxml==6.1.0
